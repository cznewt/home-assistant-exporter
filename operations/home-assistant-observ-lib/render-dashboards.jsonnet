// Grafana dashboards from the mixin (map keyed by <uid>.json). Rendered by the
// observ-lib image: `render render-dashboards.jsonnet -m dashboards_out`.
(import './mixin.libsonnet').grafanaDashboards
